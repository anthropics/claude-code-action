name: Claude Exploit Test

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: read
  issues: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: anthropics/claude-code-action@v1
